server:
  port: 8787

spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/tk_test?characterEncoding=UTF-8&serverTimezone=UTC
    username: timothy
    password: Timothy@2020

mybatis:
  type-aliases-package: com.timothy20.TKWebCrawler.dto
  mapper-locations: mybatis/**/*.xml

#  h2:
#    console:
#      enabled: true
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true
#        default_batch_fetch_size: 100
#    generate-ddl: true
#
#logging:
#  level:
#    org.hibernate.sql: debug
#    org.hibernate.type: trace